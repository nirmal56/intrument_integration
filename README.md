# intrument_integration
This python code base is specially designed for lab instrument automation.
Pre-requisists:
python should be installed in computer(>3.8).

dependencies:
pip install pyvisa-py libusb pyusb zeroconf
